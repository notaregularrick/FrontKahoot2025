import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Rojo principal (Tu color favorito: Pasión y Energía)
  static const Color primaryRed = Color(0xFFE63946);

  // Color crema para el fondo
  static const Color creamBackground = Color(0xFFFDF0D5);

  // Naranja para acentuar detalles
  static const Color orangeAccent = Color(0xFFF4A261);

  // Amarillo mostaza para otros detallitos
  static const Color mustardYellow = Color(0xFFE9C46A);

  // Azul oscuro para texto
  static const Color darkBlueText = Color(0xFF1D3557);

  static final Color textColor = Colors.grey[900]!.withOpacity(0.9);
  
}